PORT=8080

# Comma-separated list of allowed frontend origins for CORS.
FRONTEND_ORIGINS=http://localhost:5173

# The frontend origin used to build activation links emailed to users.
FRONTEND_URL=http://localhost:5173

# Full service account JSON (as a single line string), or the same
# JSON base64-encoded. Never commit the real value — set this in
# Render's environment variable settings instead.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Gmail SMTP used to send activation emails, via an App Password
# (myaccount.google.com/apppasswords — requires 2-Step Verification on
# the account). Never commit the real password — set it in Render's
# environment variable settings instead.
GMAIL_USER=
GMAIL_APP_PASSWORD=
